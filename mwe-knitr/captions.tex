%% Captions for template.Rnw
%% No R code here (possible with extra step)

\newcommand{\capRnormGroup}{
    Caption in \LaTeX (see \texttt{captions.tex}): no escapes required ($\alpha + \beta$). See also Figure \ref{fig:diamond-cut-color}.
}
